@(Html.Kendo().DropDownList()
		.Name("Statut") // The name of the widget should be the same as the name of the property.
		.DataValueField("Value")
		.DataTextField("Text")
		.BindTo((System.Collections.IEnumerable)ViewData["StatutLigne"]))
	